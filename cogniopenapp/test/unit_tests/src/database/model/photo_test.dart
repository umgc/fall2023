// TODO -- Fix Tests

void main() {
  /*
  Photo photo = createTestMediaList()[0] as Photo;

  //test code here!
  test("create a photo", () {
    expect(photo.associatedImage, isNot(null));
  });
  */
}
