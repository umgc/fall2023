import 'package:flutter_test/flutter_test.dart';
import 'package:cogniopenapp/src/galleryData.dart';
import 'package:cogniopenapp/src/video_response.dart';

void main() {
  /*
  // Add a sample video to the local to the phone gallery
  GalleryData galleryData;

  // Initialize video response object
  VideoResponse videoResponse = VideoResponse("fire extinguisher", 75, 1000);

  // Check that the Amazon Rekognition API has identified something
  test("Noticed object in video", () {
    expect(videoResponse.boundingBox!.width, greaterThan(0));
  });
  */
}
