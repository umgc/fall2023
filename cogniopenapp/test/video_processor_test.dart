import 'package:flutter_test/flutter_test.dart';
import 'package:cogniopenapp/src/galleryData.dart';
import 'package:cogniopenapp/src/video_processor.dart';

void main() {
/*
  // Call function to add items to gallery
  GalleryData galleryData;

  // Initialize video processor object
  VideoProcessor videoProcessor = new VideoProcessor();

  // Upload video
  videoProcessor.uploadVideoToS3();
  test("upload a video", () {
    expect(videoProcessor.jobId, isNot(''));
  });
  */
}
